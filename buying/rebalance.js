// how much we can buy and sell to rebalance it

// Rebalance overall base at the end of the day

// For low risk coin
// Sum and calculate percantages
// Find the percentage difference from the suggested weights
// Reweight the portfolio and multiply by current exchange rate

var importFile = require('./exchange.js');

var some_value = importFile.testFnc();
console.log("some_value");
console.log(some_value);
